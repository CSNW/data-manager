export { default as Store } from './store';
export { default as table } from './table';
export {
  filter,
  map,
  mapSeries,
  flatMap,
  sort,
  sortBy,
  clone,
  cloneSeries,
  groupBy,
  from
} from './operations';
export { compare, flow } from './utils';
